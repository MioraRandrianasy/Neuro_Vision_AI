# Base API URL used by the React frontend to target the correct backend.
# During local development this points to localhost.
# In production (e.g. deployed on Vercel), populate the Vercel project environment settings with your production FastAPI URL.
VITE_API_URL=http://127.0.0.1:8000
